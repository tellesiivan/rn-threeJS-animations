import { StatusBar } from "expo-status-bar";
import { StyleSheet, Text, View } from "react-native";
import HomeCanvas from "./source/components/HomeCanvas";

export default function App() {
  return <HomeCanvas />;
}
